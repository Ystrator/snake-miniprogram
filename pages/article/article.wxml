<!--文章详情页 - 优化版-->
<scroll-view scroll-y class="article-container {{darkMode ? 'dark-mode' : ''}}">
  <view class="article-header">
    <!-- 顶部夜间模式切换按钮 -->
    <view class="header-top">
      <view class="back-btn" bindtap="goBack">
        <text class="back-icon">←</text>
      </view>
      <view class="theme-toggle" bindtap="toggleDarkMode">
        <text class="theme-icon {{themeAnimating ? "icon-rotate" : ""}}">{{darkMode ? '☀️' : '🌙'}}</text>
      </view>
    </view>
    <text class="article-title">{{article.title}}</text>
    <view class="article-meta">
      <text class="article-category">{{article.categoryName}}</text>
      <text class="article-date">{{article.publishDate}}</text>
    </view>
    <view class="article-tags">
      <text class="tag" wx:for="{{article.tags}}" wx:key="*this" wx:for-item="tag">{{tag}}</text>
    </view>
  </view>

  <view class="article-content">
    <!-- 快速解答板块（优先显示） -->
    <view class="quick-answers-box" wx:if="{{article.quickAnswers && article.quickAnswers.length > 0}}">
      <text class="quick-answers-title">⚡ 30秒快速解答</text>
      <view class="quick-answers-list">
        <view class="qa-item" wx:for="{{article.quickAnswers}}" wx:key="*this" wx:for-index="idx">
          <text class="qa-question">Q: {{item.question}}</text>
          <text class="qa-answer">A: {{item.answer}}</text>
        </view>
      </view>
    </view>

    <!-- 摘要 -->
    <view class="article-summary-box">
      <text class="article-summary">{{article.summary}}</text>
    </view>
    
    <!-- 快速解答板块（旧版，向后兼容） -->
    <view class="quick-answer-box" wx:if="{{article.quickAnswer}}">
      <text class="quick-answer-title">{{article.quickAnswer.title}}</text>
      <view class="quick-answer-tips">
        <text class="quick-tip" wx:for="{{article.quickAnswer.tips}}" wx:key="*this">{{item}}</text>
      </view>
    </view>
    
    <!-- 详细内容 -->
    <view class="article-body">
      <view class="content-section" wx:for="{{article.content || article.sections}}" wx:key="index">
        <!-- 标题 -->
        <text class="section-heading" wx:if="{{item.type === 'heading'}}">{{item.text}}</text>

        <!-- 无序列表（新格式：text字段） -->
        <view class="section-bullet" wx:if="{{item.type === 'bullet' && item.text}}">
          <view class="bullet-item">
            <text class="bullet-dot">•</text>
            <text class="bullet-text">{{item.text}}</text>
          </view>
        </view>

        <!-- 无序列表（旧格式：items数组） -->
        <view class="section-bullet" wx:if="{{item.type === 'bullet' && item.items}}">
          <view class="bullet-item" wx:for="{{item.items}}" wx:key="*this" wx:for-item="bullet">
            <text class="bullet-dot">•</text>
            <text class="bullet-text">{{bullet}}</text>
          </view>
        </view>

        <!-- 有序列表（新格式：text字段） -->
        <view class="section-numbered" wx:if="{{item.type === 'numbered' && item.text}}">
          <view class="numbered-item">
            <text class="numbered-dot">{{index + 1}}.</text>
            <text class="numbered-text">{{item.text}}</text>
          </view>
        </view>

        <!-- 有序列表（旧格式：items数组） -->
        <view class="section-numbered" wx:if="{{item.type === 'numbered' && item.items}}">
          <view class="numbered-item" wx:for="{{item.items}}" wx:key="*this" wx:for-item="numItem">
            <text class="numbered-dot">{{index + 1}}.</text>
            <text class="numbered-text">{{numItem}}</text>
          </view>
        </view>

        <!-- 列表（带标题和描述） -->
        <view class="section-list" wx:if="{{item.type === 'list'}}">
          <view class="list-item" wx:for="{{item.items}}" wx:key="*this" wx:for-item="listItem">
            <text class="list-title">{{listItem.title}}</text>
            <text class="list-desc">{{listItem.desc}}</text>
          </view>
        </view>

        <!-- 提示信息 -->
        <view class="section-info" wx:if="{{item.type === 'info'}}">
          <text class="info-text">{{item.text}}</text>
        </view>
      </view>
    </view>
  </view>

  <view class="article-actions">
    <button class="action-btn" bindtap="toggleFavorite">
      <text>{{isFavorited ? '❤️ 已收藏' : '🤍 收藏文章'}}</text>
    </button>
    <button class="action-btn outline" bindtap="shareArticle">
      <text>📤 分享</text>
    </button>
  </view>
</scroll-view>
